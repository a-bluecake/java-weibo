<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.culr.edu.weiboproject.mapper.CommentMapper">
    <insert id="insert">
        insert into comment values (null ,#{content} ,#{created} ,#{userId} ,#{weiboId})
    </insert>
    <select id="selectByWeiboId" resultType="cn.culr.edu.weiboproject.pojo.vo.DetailsPageCommentVO">
        select comment.content as commentContent,user.nickname as commentNickname,comment.created as commentCreated
        from comment join user on comment.user_id = user.id
        where comment.weibo_id = #{weiboId}
    </select>
</mapper>